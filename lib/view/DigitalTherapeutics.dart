import 'package:flutter/material.dart';

class DigitalTherapeutics extends StatelessWidget {
  const DigitalTherapeutics({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a First Page'),
    );
  }
}
import 'package:flutter/material.dart';

class SleepNoise extends StatelessWidget {
  const SleepNoise({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is a First Page'),
    );
  }
}